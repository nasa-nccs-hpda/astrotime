source: sinusoid
dataset_root:  "${platform.project_root}/sinusoids/nc"
dataset_files:  "padded_sinusoids_*.nc"
file_size: 1000               # Number of sinusoids in a single nc file
batch_size: 50                # Batch size for training
validation_fraction: 0.1      # Fraction of training dataset that is used for validation
dset_reduction: 0.1           # Fraction of the full dataset that is used for training/validation


